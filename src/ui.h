#pragma once

#include <cstddef>

[[maybe_unused]] void render_mono_volume_bar(float volume,
                                             float sensibility,
                                             size_t line_length);
